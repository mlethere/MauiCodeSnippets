<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiCodeSnippets.Views.BirthdayPage"
             xmlns:vm="clr-namespace:MauiCodeSnippets.ViewModels"
             Title="BirthdayPage"
             >

    <!--The Movie Database example has the list of movies as a property in the MainAPp ... did not know you could do this 
        but I don't want that. I would like to have this list page oad the movies We have a binding here to BirthdayList this needs to be inititialized -->
    <ContentPage.BindingContext>
        <vm:BirthdayListViewModel />
    </ContentPage.BindingContext>

    <ListView ItemsSource="{Binding BirthdayList}" Margin="10">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Name}" x:DataType="vm:BirthdayViewModel">
                    
                </TextCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>